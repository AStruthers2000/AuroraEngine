////////////////////////////////////////////////////////////////////////////////////////////////////
/// Copyright (C) 2026 AStruthers2000 - All Rights Reserved
////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Intermediate base class for Components that participate in the render pass.
///        Derive from RenderComponent instead of Component when your Component needs to draw to
///        the screen, to allow systems to filter on render-capable Components.
////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef CORE_COMPONENTS_RENDER_COMPONENT_H
#define CORE_COMPONENTS_RENDER_COMPONENT_H

#include "core/component.h"

namespace Core
{

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Intermediate base class for renderable Components.
///
/// RenderComponent is a thin semantic layer between Component and concrete rendering Components
/// (RectRenderComponent, TextRenderComponent, etc.). It adds no data or behavior - its sole
/// purpose is to act as a common base that subsystems and queries can filter on.
////////////////////////////////////////////////////////////////////////////////////////////////////
class RenderComponent : public Component
{
public:
    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Types
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Construction parameters for RenderComponent and all derived classes.
    ///
    /// Derived classes should inherit from RenderComponent::Configuration and add their own
    /// fields:
    /// @code
    /// struct Configuration : RenderComponent::Configuration
    /// {
    ///     SDL_Color color{ 255, 255, 255, 255 };
    /// };
    /// @endcode
    ////////////////////////////////////////////////////////////////////////////////////////////////
    struct Configuration : public Component::Configuration
    {
    };


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Construction & Destruction
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Constructs a RenderComponent.
    ///
    /// @param [in] owner  - The Entity that owns this Component.
    /// @param [in] config - Optional construction parameters (update/render sort order).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    explicit RenderComponent(Entity& owner, Configuration const& config = {});
};

} // namespace Core

#endif // CORE_COMPONENTS_RENDER_COMPONENT_H
