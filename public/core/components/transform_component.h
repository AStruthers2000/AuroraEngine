////////////////////////////////////////////////////////////////////////////////////////////////////
/// Copyright (C) 2026 AStruthers2000 - All Rights Reserved
////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Stores and manages an Entity's world-space position, velocity, scale, and size.
////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef CORE_COMPONENTS_TRANSFORM_COMPONENT_H
#define CORE_COMPONENTS_TRANSFORM_COMPONENT_H

#include "core/component.h"

#include <glm/glm.hpp>

namespace Core
{

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Stores and manages an Entity's local position, velocity, scale, and size.
///
/// TransformComponent is the canonical spatial data store for any Entity that exists somewhere in
/// the game world. Rendering Components (RectRenderComponent, TextRenderComponent) look up the
/// sibling TransformComponent during on_awake() and read it during on_render() to determine where
/// and how large to draw.
///
/// @note  Exactly one TransformComponent is allowed per Entity (unique_per_entity = true).
///        Attempting to add a second one is an assertion failure.
///
/// @note  World-space accessors (get_world_position, get_world_scale) walk the parent Entity
///        chain. Cache their results if called frequently.
////////////////////////////////////////////////////////////////////////////////////////////////////
class TransformComponent : public Component
{
public:
    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Types
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Construction parameters for TransformComponent.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    struct Configuration : public Component::Configuration
    {
        /// @brief Initial local position in pixels.
        glm::vec2 position{ 0.f, 0.f };
        /// @brief Initial velocity in pixels per second.
        glm::vec2 velocity{ 0.f, 0.f };
        /// @brief Local scale factor applied to the entity's size.
        glm::vec2 scale{ 1.f, 1.f };
        /// @brief Base size of the entity in pixels (before scale).
        glm::vec2 size{ 1.f, 1.f };
    };

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Enforce at most one TransformComponent per Entity.
    ///
    /// @note  This restriction ensures all sibling Components can unambiguously locate spatial data
    ///        via @c get_sibling_component<TransformComponent>().
    ////////////////////////////////////////////////////////////////////////////////////////////////
    static constexpr bool unique_per_entity = true;


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Construction & Destruction
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Constructs a TransformComponent.
    ///
    /// @param [in] owner  - The Entity that owns this Component.
    /// @param [in] config - Optional construction parameters (position, velocity, scale, size).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    explicit TransformComponent(Entity& owner, Configuration const& config = {});

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Destructor. Default - no resources to release.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    ~TransformComponent() override = default;


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Position API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return The local position in pixels (relative to the parent Entity, or world-space if
    ///         this Entity has no parent).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    const glm::vec2& get_position() const { return m_position; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Sets the local position.
    ///
    /// @param [in] position - New local position in pixels.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_position(const glm::vec2& position) { m_position = position; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Applies a delta to the local position.
    ///
    /// @param [in] delta - Amount to add to the current local position.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void update_position(const glm::vec2& delta) { m_position += delta; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return World-space position, accumulated by walking the parent Entity chain.
    ///
    /// @note  O(depth) - cache the result if called multiple times per frame.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    glm::vec2 get_world_position() const;


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Velocity API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return The current velocity in pixels per second.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    const glm::vec2& get_velocity() const { return m_velocity; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Sets the velocity.
    ///
    /// @param [in] velocity - New velocity in pixels per second.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_velocity(const glm::vec2& velocity) { m_velocity = velocity; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Applies a delta to the velocity.
    ///
    /// @param [in] delta - Amount to add to the current velocity.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void update_velocity(const glm::vec2& delta) { m_velocity += delta; }


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Scale API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return The local scale factor.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    const glm::vec2& get_scale() const { return m_scale; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Sets the local scale factor.
    ///
    /// @param [in] scale - New scale factor (1.0, 1.0 = no scaling).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_scale(const glm::vec2& scale) { m_scale = scale; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Applies a delta to the local scale.
    ///
    /// @param [in] delta - Amount to add to the current scale.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void update_scale(const glm::vec2& delta) { m_scale += delta; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return World-space scale, accumulated by walking the parent Entity chain.
    ///
    /// @note  O(depth) - cache the result if called multiple times per frame.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    glm::vec2 get_world_scale() const;


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Size API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @return The base size in pixels, before scale is applied.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    const glm::vec2& get_size() const { return m_size; }

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Sets the base size.
    ///
    /// @param [in] size - New base size in pixels (before scale).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_size(const glm::vec2& size) { m_size = size; }

private:
    /// @brief Local position in pixels.
    glm::vec2 m_position{};

    /// @brief Velocity in pixels per second.
    glm::vec2 m_velocity{ 0.f, 0.f };

    /// @brief Local scale factor.
    glm::vec2 m_scale{};

    /// @brief Base size in pixels (before scale).
    glm::vec2 m_size{};
};

} // namespace Core

#endif // CORE_COMPONENTS_TRANSFORM_COMPONENT_H
