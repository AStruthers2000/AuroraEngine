////////////////////////////////////////////////////////////////////////////////////////////////////
/// Copyright (C) 2026 AStruthers2000 - All Rights Reserved
////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief A self-contained Entity that renders a colored rectangle with an optional border.
///        Intended as a background or container for composite UI layouts.
////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef CORE_ENTITIES_UI_PANEL_H
#define CORE_ENTITIES_UI_PANEL_H

#include "core/entities/ui/ui_element.h"

#include <glm/glm.hpp>

namespace Core
{
class TransformComponent;
class RectRenderComponent;
}

namespace Core::UI
{

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief A self-contained Entity that renders a colored rectangle with an optional border.
///
/// Panel encapsulates a TransformComponent, an AnchorComponent, and a RectRenderComponent into a
/// ready-to-use UI background or container. Construct it via Layer::add_entity<Panel>() and pass
/// a Configuration to set the size, fill color, and optional border.
////////////////////////////////////////////////////////////////////////////////////////////////////
class Panel : public UIElement
{
public:
    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Types
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Construction parameters for Panel.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    struct Configuration : UIElement::Configuration
    {
        glm::vec2 panel_size{ 0.f, 0.f };
        SDL_Color panel_color{ 255, 255, 255, 255 };
        bool has_border{ false };
        glm::vec2 border_size{ 0.f, 0.f };
        SDL_Color border_color{ 100, 100, 100, 255 };
    };


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Construction & Destruction
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Constructs a Panel.
    ///
    /// @param [in] owner  - The Layer or parent Entity that will own this Panel.
    /// @param [in] config - Optional construction parameters.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    Panel(Entity::Owner owner, Configuration const& config = {});


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Virtual Lifecycle Hook Overrides
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Renders the panel rectangle and, when enabled, the border outline.
    ///        Called each frame by the engine's render pass.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void on_render(SDL_Renderer* renderer) override;


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Runtime API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the fill color of the panel at runtime.
    ///
    /// @param [in] new_color - New RGBA fill color.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_panel_color(SDL_Color const& new_color);

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Shows or hides the border outline at runtime.
    ///
    /// @param [in] has_border - @c true to show the border, @c false to hide it.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_has_border(bool has_border);

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the border thickness at runtime.
    ///
    /// @param [in] new_border_size - New border size in pixels (width and height of the border band).
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_border_size(glm::vec2 const& new_border_size);

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the border color at runtime.
    ///
    /// @param [in] new_color - New RGBA border color.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_border_color(SDL_Color const& new_color); 

private:
    SDL_Color m_panel_color{};
    bool m_has_border{};
    glm::vec2 m_border_size{};
    SDL_Color m_border_color{};

    /// @brief Cached panel Transform.
    std::weak_ptr<TransformComponent> m_transform{};

    /// @brief Cached panel foreground.
    std::weak_ptr<RectRenderComponent> m_panel{};

};

} // namespace Core::UI

#endif // CORE_ENTITIES_UI_PANEL_H
