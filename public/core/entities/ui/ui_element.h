////////////////////////////////////////////////////////////////////////////////////////////////////
/// Copyright (C) 2026 AStruthers2000 - All Rights Reserved
////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Shared base class for all UI elements (i.e. Label, Panel, etc.)
////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef CORE_ENTITIES_UI_UI_ELEMENT_H
#define CORE_ENTITIES_UI_UI_ELEMENT_H

#include "core/entity.h"
#include "core/components/anchor_component.h"

#include <glm/glm.hpp>

namespace Core::UI
{

////////////////////////////////////////////////////////////////////////////////////////////////////
/// @brief Shared base class for all UI elements (Label, Panel, etc.). Provides anchor-based
///        positioning relative to a parent Entity.
///
/// UIElement adds an AnchorComponent to any derived Entity, wiring it up with the @c self_anchor,
/// @c parent_anchor, and @c offset values from its Configuration. Construct concrete types such as
/// Label or Panel rather than UIElement directly.
///
/// @note  With the default @c self_anchor / @c parent_anchor of Anchor::TopLeft, the anchor math
///        collapses to just @c offset, so a plain top-left-relative position is just
///        `.offset = { x, y }` with the other fields left at their defaults.
////////////////////////////////////////////////////////////////////////////////////////////////////
class UIElement : public Entity
{
public:
    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Types
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Construction parameters for Label. Inherits @c update_order from
    ///        Entity::Configuration.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    struct Configuration : Entity::Configuration
    {
        /// @brief Pivot point on the Label's own bounding box. See AnchorComponent.
        Anchor self_anchor{ Anchor::TopLeft };
        /// @brief Reference point on the parent Entity's bounding box. See AnchorComponent.
        Anchor parent_anchor{ Anchor::TopLeft };
        /// @brief Fine-tuning offset in pixels, applied after both anchors are resolved. With
        ///        default TopLeft anchors, this is equivalent to a plain world-space position.
        glm::vec2 offset{ 0.f, 0.f };
    };


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Construction & Destruction
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Constructs a UIElement base class.
    ///
    /// @param [in] owner  - The Layer or parent Entity that will own this Label.
    /// @param [in] config - Optional construction parameters.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    UIElement(Entity::Owner owner, Configuration const& config = {});


    ////////////////////////////////////////////////////////////////////////////////////////////////
    // Runtime API
    ////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the pivot point on this Entity's own bounding box.
    ///
    /// @param [in] anchor - New self anchor.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_self_anchor(Anchor anchor);

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the reference point on the parent Entity's bounding box.
    ///
    /// @param [in] anchor - New parent anchor.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_parent_anchor(Anchor anchor);

    ////////////////////////////////////////////////////////////////////////////////////////////////
    /// @brief Changes the fine-tuning offset applied after both anchors are resolved.
    ///
    /// @param [in] offset - New offset in pixels.
    ////////////////////////////////////////////////////////////////////////////////////////////////
    void set_offset(glm::vec2 offset);

private:
    /// @brief Cached AnchorComponent.
    std::weak_ptr<AnchorComponent> m_anchor{};
};

} // namespace Core::UI

#endif // CORE_ENTITIES_UI_UI_ELEMENT_H
